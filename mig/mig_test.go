package mig

import (
    "testing"
)

func TestNewMig(t *testing.T) {
    // --- Given ---
    //fs := afero.NewMemMapFs()
    //fs.MkdirAll("a/b", 0755)
    //afero.WriteFile(fs, "a/b/mig.yaml", []byte(tstYAML), 0644)
    //cfg, _ := NewConfig(fs, "a/b/mig.yaml")
    //trg, _ := cfg.Target("setup")

    // --- When ---

    // --- Then ---

}
